# Activity 02 — Handle an event

Timebox: 6 minutes.

1. Before looking at `greeting.js`, predict which HTML elements JavaScript needs to select.
2. Change the successful greeting to include a second sentence.
3. Add a reset button that clears the result and returns the count to zero.

Done when submitting a name updates the page without reloading it, blank input shows an error, repeated submissions update the count, and the reset action restores the initial UI.

