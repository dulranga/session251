# Activity 04 — Think in state

Timebox: 6 minutes.

1. Identify every `useState` value and point to the JSX that it controls.
2. Change the reset action so it returns keyboard focus to the name input.
3. Add a second `ActionButton` somewhere on the page to demonstrate reuse.

Done when the greeting and counter update without direct DOM queries, blank input still shows an error, and Reset restores the initial state.

