# Activity 06 — Read and change utility classes

Timebox: 6 minutes.

1. Match five utilities in `GreetingApp.jsx` to declarations in checkpoint 05's `styles.css`.
2. Change the card's spacing at the default and `sm` breakpoints.
3. Add visible hover and focus-visible styles to the Clear history button.
4. Change one status color in the static `statusClasses` map.

Done when the layout works at mobile and desktop widths, states retain distinct colors, keyboard focus remains visible, and no application behavior changed.

